import React from 'react';
import LoginForm from '../components/LoginForm'; // Assuming you saved the LoginForm as LoginForm.js

const LoginPage = () => {
    return (
        <div>
            <h1>Welcome to MediSense AI</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
